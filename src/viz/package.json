{
  "name": "hd-map-pipeline-viz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n api,vite -c blue,cyan \"python3 ../../src/api/server.py\" \"vite\"",
    "dev:viz": "vite",
    "build": "tsc && vite build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "three": "^0.182.0",
    "vite": "^8.0.0"
  },
  "devDependencies": {
    "@types/three": "^0.182.0",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.0"
  }
}
